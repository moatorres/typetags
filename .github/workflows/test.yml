name: test

on:
  pull_request:
    branches:
      - master
      - next
    paths:
      - 'lib/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn && yarn test
